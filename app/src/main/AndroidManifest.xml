<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="br.edu.ifpr.pedro.vikingsactivity">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".AthelstanActivity"
                android:label="@string/title_activity_athelstan"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".BjornActivity">
        </activity>
        <activity android:name=".FlokiActivity">
        </activity>
        <activity android:name=".LagerthaActivity">
        </activity>
        <activity android:name=".RagnarActivity">
        </activity>
        <activity android:name=".RolloActivity">
        </activity>
    </application>

</manifest>